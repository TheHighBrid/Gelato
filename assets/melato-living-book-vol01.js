(() => {
  'use strict';

  const CDN = 'https://cdn.shopify.com/s/files/1/0809/3358/5151/files';
  const DATA = {"FRAMES":{"tawakal1":{"id":"tawakal1","file":"MELATO_TAWAKAL26_TLB_OTTAWA_001.jpg","v":"1788317043","title":"Tawakal","kicker":"The Uniform","caption":"Two people, one silhouette. The M is not a logo here \u2014 it is a handshake.","place":"Ottawa","garment":"Velour track set","shopHref":"/collections/tracksuits","shopLabel":"Shop the uniform"},"tawakal2":{"id":"tawakal2","file":"MELATO_TAWAKAL26_TLB_OTTAWA_002.jpg","v":"1788317085","title":"Tawakal, take two","kicker":"The Uniform","caption":"Trust the set. The colourway does the talking so you don\u2019t have to.","place":"Ottawa","garment":"Velour track set","shopHref":"/collections/tracksuits","shopLabel":"Shop tracksuits"},"tawakal3":{"id":"tawakal3","file":"MELATO_TAWAKAL26_TLB_OTTAWA_003.png","v":"1788317198","title":"Tawakal, still","kicker":"The Uniform","caption":"If a tracksuit can hold a stare, it can hold a city.","place":"Ottawa","shopHref":"/collections/tracksuits","shopLabel":"Shop the uniform"},"burgundy":{"id":"burgundy","file":"The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_4.png","v":"1780275808","title":"Close enough to count the pile","kicker":"Velour","caption":"Burgundy velour in daylight is a love letter with a zipper.","garment":"Melato velour jacket","shopHref":"/collections/tracksuits","shopLabel":"Shop velour"},"colourblock":{"id":"colourblock","file":"The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_18.png","v":"1780276006","title":"Colourblock as personality test","kicker":"The Set","caption":"If your tracksuit has more opinions than you do, congratulations. You bought correctly.","garment":"Colourblock velour set","shopHref":"/collections/tracksuits","shopLabel":"Shop the set"},"colourblock2":{"id":"colourblock2","file":"The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_12.png","v":"1780276053","title":"The argument in stripes","kicker":"The Set","caption":"A colourway is a thesis. This one passed with distinction.","shopHref":"/collections/tracksuits","shopLabel":"Shop tracksuits"},"casting":{"id":"casting","file":"The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_26.png","v":"1780275744","title":"In character","kicker":"Cinema","caption":"The uniform is a role. The wearer writes the scene.","garment":"Colourblock shell"},"ojos":{"id":"ojos","file":"The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_23.png","v":"1780276102","title":"OJOS \u00b7 Drop 002","kicker":"Drop Archive","caption":"Eyes first. Everything else is supporting cast.","garment":"OJOS velour jacket","shopHref":"/products/ojos-velour-track-jacket","shopLabel":"Shop OJOS"},"likenever":{"id":"likenever","file":"Melato.ca__EL_TLL_FRAME_60__AP.jpg","v":"1787223017","title":"Like Never","kicker":"Hardware","caption":"A pendant that refuses the past tense. Comme never change \u2014 just spell it louder.","garment":"Like Never chain"},"intent":{"id":"intent","file":"The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_27.png","v":"1780276158","title":"Cut with intent","kicker":"The Uniform","caption":"Intent is a seam allowance you can feel from across the room.","shopHref":"/collections/tracksuits","shopLabel":"Shop tracksuits"},"ciao":{"id":"ciao","file":"MELATO_CIAO26_TLB_TORONTO_001.png","v":"1788317256","title":"Ciao","kicker":"Toronto","caption":"Leaving looks better when the shirt says Melato from behind. Toronto, dusk, one bag, no explanation.","place":"Toronto","garment":"Bowling shirt","shopHref":"/collections/new-arrivals","shopLabel":"Shop new arrivals"},"bonjour":{"id":"bonjour","file":"MELATO_BONJOUR26_TLB_MONTREAL_001.png","v":"1788316983","title":"Bonjour","kicker":"Montr\u00e9al","caption":"A dome, a dress, a bag with an M that does not whisper. Montr\u00e9al after dark is a language.","place":"Montr\u00e9al","garment":"Belted cargo dress \u00b7 M bag","shopHref":"/collections/new-arrivals","shopLabel":"Shop the night edit"},"casino":{"id":"casino","file":"MELATO_CASINO26_TLB_MONTREAL_001.png","v":"1788317285","title":"House always dressed","kicker":"Montr\u00e9al","caption":"Escalators are just runways with better lighting. She already knew.","place":"Montr\u00e9al","garment":"Black tracksuit","shopHref":"/collections/tracksuits","shopLabel":"Shop the uniform"},"enroute1":{"id":"enroute1","file":"MELATO_ENROUTE26_TLB_OTTAWA_001.png","v":"1788385077","title":"En route","kicker":"Ottawa","caption":"The O-Train to Blair. Heels on the yellow line. A tracksuit that commutes better than you do.","place":"Ottawa \u00b7 Confederation Line","garment":"Melato track set","shopHref":"/collections/tracksuits","shopLabel":"Shop tracksuits"},"enroute2":{"id":"enroute2","file":"MELATO_ENROUTE26_TLB_OTTAWA_002.png","v":"1788385134","title":"Still en route","kicker":"Ottawa","caption":"Public transit, private uniform. Proudly 100% Canadian \u2014 the poster said it first.","place":"Ottawa","shopHref":"/collections/tracksuits","shopLabel":"Shop the uniform"},"transit":{"id":"transit","file":"The_Living_Lookbook_Editorial_Shots_Vol2_15.jpg","v":"1783807065","title":"Platform 1","kicker":"Ottawa","caption":"If the train is late, at least the silhouette is on time.","place":"Ottawa","shopHref":"/collections/tracksuits","shopLabel":"Shop tracksuits"},"vol5":{"id":"vol5","file":"The_Living_Lookbook_Editorial_Shots_Vol2_5.jpg","v":"1783807294","title":"Presence","kicker":"The Uniform","caption":"Some frames don\u2019t ask to be looked at. They assume you already are.","shopHref":"/collections/tracksuits","shopLabel":"Shop the uniform"},"vol20":{"id":"vol20","file":"The_Living_Lookbook_Editorial_Shots_Vol2_20.jpg","v":"1783806848","title":"Drop 001, still walking","kicker":"City Circuit","caption":"Limited production. Unlimited pavement.","shopHref":"/collections/tracksuits","shopLabel":"Shop Drop 001"},"riad":{"id":"riad","file":"MELATO_ORIGINS26_TLB_Marrakech_Riad_001.jpg","v":"1788086046","title":"Riad, with citrus","kicker":"Marrakech","caption":"A courtyard, a pool, a bird on the roof, burgundy tape down the leg. The bloodline has a postcode.","place":"Marrakech","garment":"Burgundy-tape velour set","shopHref":"/collections/tracksuits","shopLabel":"Shop the set"},"meknesDress":{"id":"meknesDress","file":"MELATO_ORIGINS26_DRESS_CODE_VIOLATION_RIB_KNIT_POLO_MINI_DRESS_TLB_MEKNES_SALON_001.jpg","v":"1788085820","title":"Dress Code Violation","kicker":"Meknes","caption":"A salon, a lantern, a zip that starts conversations. The dress is the violation. The room is the alibi.","place":"Meknes","garment":"Rib knit polo mini","shopHref":"/products/dress-code-rib-knit-zip-mini-dress","shopLabel":"Shop the dress"},"rabat":{"id":"rabat","file":"MELATO_ORIGINS26_TLB_RABAT_001.jpg","v":"1788317165","title":"Rabat, leaning","kicker":"Rabat","caption":"Stone, iron, sky. The set takes the balcony like it pays rent.","place":"Rabat","garment":"Burgundy-tape velour set","shopHref":"/collections/tracksuits","shopLabel":"Shop tracksuits"},"casa":{"id":"casa","file":"MELATO_ORIGINS26__TLB_CASABLANCA_ROOFTOP_001.jpg","v":"1788085721","title":"Casablanca rooftop","kicker":"Casablanca","caption":"The city below. The uniform above. A rooftop is just a better front row.","place":"Casablanca","garment":"Burgundy-tape velour set","shopHref":"/collections/tracksuits","shopLabel":"Shop the set"},"meknes3":{"id":"meknes3","file":"MELATO_ORIGINS26_TLB_MEKNES_003.jpg","v":"1788317464","title":"Meknes, third frame","kicker":"Meknes","caption":"Origins are not a moodboard. They are a city that still answers the phone.","place":"Meknes","shopHref":"/collections/tracksuits","shopLabel":"Shop the uniform"},"meknes6":{"id":"meknes6","file":"MELATO_ORIGINS26_TLB_MEKNES_006.jpg","v":"1788317447","title":"Meknes, sixth","kicker":"Meknes","caption":"Same sun. Different column. The set refuses to be a tourist.","place":"Meknes"},"detour":{"id":"detour","file":"MELATO_ORIGINS26_DETOUR-TEE_TLB_FOUR-SEASONS_001.jpg","v":"1787780698","title":"Detour Tee","kicker":"Four Seasons","caption":"A tee that took the long way. Four Seasons, one detour, no apology.","place":"Four Seasons","garment":"Detour tee","shopHref":"/collections/new-arrivals","shopLabel":"Shop new arrivals"},"redlight":{"id":"redlight","file":"MELATO_REDLIGHT26_TLL_OTTAWA_002.png","v":"1788382795","title":"Redlight","kicker":"After Hours","caption":"One slit of light. The rest is implication. Ottawa after midnight is a different country.","place":"Ottawa"},"silk":{"id":"silk","file":"The_Living_Lookbook_Melato_Editorial_Lookbook_ACT_3_SHOT_1_www.melato.ca.jpg","v":"1784775020","title":"The correspondence","kicker":"Blush Ledger","caption":"Silk, wine, a letter that will not send itself. Some shirts know what you did.","garment":"Blush silk shirt","shopHref":"/collections/new-arrivals","shopLabel":"Shop the shirt"},"letter":{"id":"letter","file":"The_Living_Lookbook_Melato_Editorial_Lookbook_ACT_3_SHOT_3_www.melato.ca.jpg","v":"1784775257","title":"Structured grace","kicker":"The Ledger","caption":"Burnt edge, wax M, lace that saw too much. The archive files feelings as objects.","garment":"Melato correspondence"},"sunday":{"id":"sunday","file":"The_Living_Lookbook_Melato_Editorial_Lookbook_ACT_3_SHOT_4_www.melato.ca.jpg","v":"1784779568","title":"Sunday edition","kicker":"Last Light","caption":"The quietest frame in the book. It still has a spine."},"gloves":{"id":"gloves","file":"The_Living_Lookbook_Melato_Editorial_Lookbook_ACT_3_SHOT_7_www.melato.ca.jpg","v":"1784779661","title":"Morning ritual","kicker":"The Ledger","caption":"Two gloves. One seal. The correspondence continues whether you open it or not."},"mano":{"id":"mano","file":"MELATO_MANODIMANO26_TLB_OTTAWA_001.png","v":"1788384910","title":"Mano di mano","kicker":"Ottawa","caption":"A hand in mesh is still a hand. Soft power, literally.","place":"Ottawa"},"arnibal":{"id":"arnibal","file":"MELATO_ARNIBAL26_TLL_OTTAWA_002.jpg","v":"1788386378","title":"Arnibal","kicker":"After Hours","caption":"She brought the camera. The red wall brought the plot. Press record.","place":"Ottawa","garment":"Black knit \u00b7 cinched waist"},"afterdark":{"id":"afterdark","file":"The_Living_Lookbook_Editorial_Shots_Vol2_26.jpg","v":"1783806623","title":"After dark","kicker":"EvesWardrobe","caption":"The night edit does not clock out. It just changes the bulb.","shopHref":"/collections/new-arrivals","shopLabel":"Shop new arrivals"},"afterhours":{"id":"afterhours","file":"Melato.ca_EL_TLL_FRAME_105.jpg","v":"1787293058","title":"After hours","kicker":"Velour Sets","caption":"When the lights go down, velour gets louder.","shopHref":"/collections/tracksuits","shopLabel":"Shop velour"},"obsidian":{"id":"obsidian","file":"MELATO.CA_OBSIDIAN26_TLL_OTTAWA_001.png","v":"1788388020","title":"Obsidian Coast","kicker":"Objects","caption":"The smell of a closed door. Eau de parfum, held like a verdict.","place":"Ottawa","garment":"Obsidian Coast"},"stillLife":{"id":"stillLife","file":"Melato.ca_EL_TLL_FRAME_106.png","v":"1787298240","title":"The uniform, unpacked","kicker":"Objects","caption":"Fur, braces, hardware. Not everything that holds you is a garment. Some things just look like a scene.","garment":"Rex X fur \u00b7 Melato braces","shopHref":"/products/rex-x-fur-jacket-1","shopLabel":"Shop Rex X"},"unbothered":{"id":"unbothered","file":"The_Living_Lookbook-Frame-0_1.jpg","v":"1781421994","title":"Unbothered","kicker":"Last Light","caption":"The last page is a face that already left the building.","shopHref":"/collections/new-arrivals","shopLabel":"Shop new arrivals"},"frame0":{"id":"frame0","file":"The_Living_Lookbook-Frame-0_1.jpg","v":"1781421994","title":"Frame zero","kicker":"Last Light","caption":"Before the chapter titles, there was a look. It still holds."},"frame8":{"id":"frame8","file":"The_Living_Lookbook-Frame-0_8.jpg","v":"1781422012","title":"The departure","kicker":"Last Light","caption":"Clothes leaving the frame. You stay in them.","shopHref":"/collections/tracksuits","shopLabel":"Shop tracksuits"},"frame10":{"id":"frame10","file":"The_Living_Lookbook-Frame-0_8.jpg","v":"1781422012","title":"Late exit","kicker":"Last Light","caption":"The door is a better ending than a logo."},"identity":{"id":"identity","file":"Melato.ca__EL_TLL_FRAME_50.jpg","v":"1788560121","title":"Identity uniform","kicker":"The Uniform","caption":"Clothing as a language for identity \u2014 not a substitute for it.","shopHref":"/collections/tracksuits","shopLabel":"Shop the uniform"},"nightedit":{"id":"nightedit","file":"Melato.ca__EL_TLL_FRAME_61.jpg","v":"1787150480","title":"The night edit","kicker":"After Hours","caption":"Same city. Different wattage.","shopHref":"/collections/new-arrivals","shopLabel":"Shop new arrivals"},"culture":{"id":"culture","file":"Melato.ca__EL_TLL_FRAME_84.jpg","v":"1787152701","title":"Culture forward","kicker":"EvesWardrobe","caption":"The culture wears us. We just cut the pattern.","shopHref":"/collections/new-arrivals","shopLabel":"Shop new arrivals"},"archive":{"id":"archive","file":"Melato.ca__EL_TLL_FRAME_34.jpg","v":"1787024272","title":"Drop archive","kicker":"The Uniform","caption":"When a colourway is gone, it becomes part of the story. That is the point.","shopHref":"/collections/tracksuits","shopLabel":"Shop the archive"},"statement":{"id":"statement","file":"Melato.ca__EL_TLL_FRAME_85.jpg","v":"1787152725","title":"The statement","kicker":"Velour","caption":"Not every frame needs a caption. This one asked nicely.","shopHref":"/collections/tracksuits","shopLabel":"Shop velour"}},"CHAPTERS":[{"id":"uniform","roman":"I","numeral":"01","title":"The Uniform","subtitle":"Identity, zipped","thesis":"A tracksuit is a handshake you can sit down in.","coverId":"tawakal1"},{"id":"cities","roman":"II","numeral":"02","title":"City Circuit","subtitle":"Ciao \u00b7 Bonjour \u00b7 En route","thesis":"Three cities. One silhouette. Different greetings.","coverId":"bonjour"},{"id":"origins","roman":"III","numeral":"03","title":"Origins","subtitle":"The bloodline has a courtyard","thesis":"Meknes, Rabat, Marrakech, Casablanca \u2014 the set goes home.","coverId":"riad"},{"id":"hours","roman":"IV","numeral":"04","title":"After Hours","subtitle":"The night department","thesis":"The night is just a better lighting department.","coverId":"redlight"},{"id":"objects","roman":"V","numeral":"05","title":"Objects of Intent","subtitle":"Still lives, still living","thesis":"Not everything that holds you is a garment.","coverId":"obsidian"},{"id":"last","roman":"VI","numeral":"06","title":"Last Light","subtitle":"Clothes leaving the frame","thesis":"When a colourway is gone, it becomes part of the story.","coverId":"unbothered"}],"SPREADS":[{"id":"contents","kind":"contents"},{"id":"u-title","kind":"title","chapterId":"uniform","frameIds":["tawakal1"],"kicker":"Chapter I","title":"The Uniform","body":"Tailored for movement. Cut with intent. Worn like a civic costume."},{"id":"u-gate","kind":"gatefold","chapterId":"uniform","frameIds":["burgundy"],"kicker":"Velour, close","title":"The pile is the point","body":"Daylight on burgundy is not casual. It is a manifesto with a zipper."},{"id":"u-diptych","kind":"diptych","chapterId":"uniform","frameIds":["colourblock","likenever"]},{"id":"u-quote","kind":"quote","chapterId":"uniform","quote":"The culture wears us. We just cut the pattern.","quoteAttr":"Melato \u00b7 Est. 2026"},{"id":"u-sheet","kind":"sheet","chapterId":"uniform","frameIds":["tawakal2","ojos","intent","identity","casting","colourblock2"]},{"id":"c-title","kind":"title","chapterId":"cities","frameIds":["bonjour"],"kicker":"Chapter II","title":"City Circuit","body":"Ottawa says en route. Toronto says ciao. Montr\u00e9al says bonjour \u2014 and means it."},{"id":"c-gate","kind":"gatefold","chapterId":"cities","frameIds":["ciao"],"kicker":"Toronto at dusk","title":"Leaving, with luggage","body":"A bowling shirt that writes Melato across the back so the city can read it without turning you around."},{"id":"c-diptych","kind":"diptych","chapterId":"cities","frameIds":["enroute1","casino"]},{"id":"c-trio","kind":"trio","chapterId":"cities","frameIds":["transit","enroute2","vol20"]},{"id":"o-title","kind":"title","chapterId":"origins","frameIds":["riad"],"kicker":"Chapter III","title":"Origins","body":"A riad pool, a citrus tree, a bird on the roof. Notes from the bloodline."},{"id":"o-gate","kind":"gatefold","chapterId":"origins","frameIds":["meknesDress"],"kicker":"Meknes salon","title":"Dress Code Violation","body":"The dress is the violation. The lantern is the witness. The zip is the plot."},{"id":"o-diptych","kind":"diptych","chapterId":"origins","frameIds":["rabat","casa"]},{"id":"o-sheet","kind":"sheet","chapterId":"origins","frameIds":["meknes3","meknes6","detour","vol5"]},{"id":"h-title","kind":"title","chapterId":"hours","frameIds":["redlight"],"kicker":"Chapter IV","title":"After Hours","body":"Casino stairs. Red slits of light. A letter that will not send itself."},{"id":"h-gate","kind":"gatefold","chapterId":"hours","frameIds":["silk"],"kicker":"Blush ledger","title":"The correspondence","body":"Some shirts know what you did. The candle is just there for lighting continuity."},{"id":"h-diptych","kind":"diptych","chapterId":"hours","frameIds":["arnibal","letter"]},{"id":"h-trio","kind":"trio","chapterId":"hours","frameIds":["afterdark","nightedit","afterhours"]},{"id":"ob-title","kind":"title","chapterId":"objects","frameIds":["obsidian"],"kicker":"Chapter V","title":"Objects of Intent","body":"Perfume. Braces. Wax. A hand in mesh. The wardrobe extends past cloth."},{"id":"ob-diptych","kind":"diptych","chapterId":"objects","frameIds":["stillLife","mano"]},{"id":"ob-portrait","kind":"portrait","chapterId":"objects","frameIds":["gloves"],"kicker":"Filed as object","title":"Pass it on","body":"Two gloves, one seal, no return address. The living book keeps correspondence."},{"id":"l-title","kind":"title","chapterId":"last","frameIds":["unbothered"],"kicker":"Chapter VI","title":"Last Light","body":"Clothes leaving the frame. You stay in them."},{"id":"l-quote","kind":"quote","chapterId":"last","quote":"When a colourway is gone, it becomes part of the story.","quoteAttr":"The Melato Archive \u00b7 Limited production"},{"id":"l-diptych","kind":"diptych","chapterId":"last","frameIds":["frame8","frame10"]},{"id":"l-sheet","kind":"sheet","chapterId":"last","frameIds":["sunday","frame0","culture","archive","statement","tawakal3"]},{"id":"colophon","kind":"colophon"}],"MARQUEE":["THE CULTURE WEARS US","CUT WITH INTENT","COMME NEVER CHANGE","MELATO","LIMITED PRODUCTION","DROP 001 \u00b7 DROP 002","THE CULTURE WEARS US","CUT WITH INTENT","LIKE NEVER CHANGE"],"SHOP":{"all":"/collections/all","tracksuits":"/collections/tracksuits","new":"/collections/new-arrivals","dress":"/products/dress-code-rib-knit-zip-mini-dress","fur":"/products/rex-x-fur-jacket-1","ojos":"/products/ojos-velour-track-jacket","home":"/","story":"/pages/our-story","notes":"/pages/newsletter"},"SPLASH":[[["Melato.ca_EL_TLL_FRAME_106.png","1787298240"],["The_Living_Lookbook_Editorial_Shots_Vol2_15.jpg","1783807065"],["The_Living_Lookbook_Editorial_Shots_Vol2_5.jpg","1783807294"]],[["The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_4.png","1780275808"],["The_Living_Lookbook_Melato_Editorial_Lookbook_ACT_3_SHOT_7_www.melato.ca.jpg","1784779661"],["The_Living_Lookbook_Melato_Editorial_Lookbook_ACT_3_SHOT_1_www.melato.ca.jpg","1784775020"]],[["The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_26.png","1780275744"],["The-Living-Lookbook-Editorial-Photoshoot-Frames-by-Melato_18.png","1780276006"],["MELATO_TAWAKAL26_TLB_OTTAWA_001.jpg","1788317043"]],[["Melato.ca__EL_TLL_FRAME_60__AP.jpg","1787223017"],["MELATO_CASINO26_TLB_MONTREAL_001.png","1788317285"],["Melato.ca__EL_TLL_FRAME_84.jpg","1787152701"]],[["MELATO_ORIGINS26_TLB_Marrakech_Riad_001.jpg","1788086046"],["The_Living_Lookbook_Melato_Editorial_Lookbook_ACT_3_SHOT_3_www.melato.ca.jpg","1784775257"],["Melato.ca_EL_TLL_FRAME_105.jpg","1787293058"]]]};
  const WHEEL_MS = 720;
  const SWIPE_PX = 48;
  const SPREADS = DATA.SPREADS;
  const CHAPTERS = DATA.CHAPTERS;
  const FRAMES = DATA.FRAMES;
  const TOTAL = SPREADS.length;
  const SHOP = DATA.SHOP;

  const ICON = {
    left: '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M15 18l-6-6 6-6"/></svg>',
    right: '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M9 18l6-6-6-6"/></svg>',
    list: '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M8 6h13M8 12h13M8 18h13M3 6h.01M3 12h.01M3 18h.01"/></svg>',
    close: '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M18 6L6 18M6 6l12 12"/></svg>'
  };

  function esc(value) {
    return String(value == null ? '' : value)
      .replace(/[&<>"']/g, function (ch) {
        if (ch === '&') return '&' + 'amp;';
        if (ch === '<') return '&' + 'lt;';
        if (ch === '>') return '&' + 'gt;';
        if (ch === '"') return '&' + 'quot;';
        return '&#39;';
      });
  }

  function frameSrc(file, v, width) {
    return CDN + '/' + encodeURIComponent(file) + '?v=' + v + '&width=' + (width || 1400);
  }

  function srcset(file, v) {
    return [480, 800, 1200, 1800].map(function (w) {
      return frameSrc(file, v, w) + ' ' + w + 'w';
    }).join(', ');
  }

  function frameById(id) {
    return FRAMES[id];
  }

  function chapterById(id) {
    return CHAPTERS.find(function (c) { return c.id === id; });
  }

  function chapterPageIndex(id) {
    return SPREADS.findIndex(function (s) { return s.chapterId === id && s.kind === 'title'; });
  }

  function framesIn(spread) {
    return (spread.frameIds || []).map(frameById).filter(Boolean);
  }

  function imgTag(frame, opts) {
    opts = opts || {};
    const width = opts.width || 1400;
    const sizes = opts.sizes || '(max-width: 700px) 100vw, 70vw';
    const ken = opts.ken ? ' lbv-ken' : '';
    const eager = opts.eager ? 'eager' : 'lazy';
    const extra = opts.className ? ' ' + opts.className : '';
    const alt = esc(frame.title + (frame.place ? ' — ' + frame.place : ''));
    return '<img class="lbv-img' + ken + extra + '" src="' + esc(frameSrc(frame.file, frame.v, width)) + '" srcset="' + esc(srcset(frame.file, frame.v)) + '" sizes="' + esc(sizes) + '" alt="' + alt + '" loading="' + eager + '" decoding="async" onerror="this.classList.add(\'is-missing\');this.removeAttribute(\'srcset\');this.removeAttribute(\'src\')">';
  }

  function placeKicker(frame) {
    return '<div class="lbv-place"><p class="k">' + esc(frame.kicker) + '</p><p class="t">' + esc(frame.title) + '</p>' +
      (frame.place ? '<p class="p">' + esc(frame.place) + '</p>' : '') + '</div>';
  }

  function openable(frame, siblings, opts) {
    opts = opts || {};
    return '<button type="button" class="lbv-open" data-open="' + esc(frame.id) + '" data-siblings="' + esc((siblings || []).map(function (f) { return f.id; }).join(',')) + '">' +
      imgTag(frame, opts) + (opts.inner || '') + '</button>';
  }

  function renderSpread(spread) {
    const frames = framesIn(spread);
    const chapter = spread.chapterId ? chapterById(spread.chapterId) : null;
    switch (spread.kind) {
      case 'contents':
        return renderContents();
      case 'title':
        return renderTitle(spread, frames, chapter);
      case 'gatefold':
        return renderGate(spread, frames);
      case 'diptych':
        return renderDiptych(frames);
      case 'trio':
        return renderTrio(frames);
      case 'quote':
        return '<div class="lbv-quote"><span class="lbv-quote-mark" aria-hidden="true">“</span><figure><blockquote>' + esc(spread.quote) + '</blockquote><figcaption>' + esc(spread.quoteAttr) + '</figcaption></figure></div>';
      case 'sheet':
        return renderSheet(frames);
      case 'portrait':
        return renderPortrait(spread, frames);
      case 'colophon':
        return renderColophon();
      default:
        return '';
    }
  }

  function renderContents() {
    const cards = CHAPTERS.map(function (ch) {
      const cover = frameById(ch.coverId);
      return '<li><button type="button" class="lbv-chapter-card" data-jump="' + esc(ch.id) + '">' +
        '<div class="media"><img class="lbv-img" src="' + esc(frameSrc(cover.file, cover.v, 700)) + '" alt="" loading="lazy"></div>' +
        '<div class="copy"><div><p class="roman">' + esc(ch.roman) + ' · ' + esc(ch.numeral) + '</p><h3>' + esc(ch.title) + '</h3><p class="sub">' + esc(ch.subtitle) + '</p></div>' +
        '<p class="thesis">' + esc(ch.thesis) + '</p></div></button></li>';
    }).join('');
    return '<div class="lbv-contents"><header class="lbv-contents-head"><div><p class="eye">Vol. 01 · 2026</p><h2>The Living Book</h2></div>' +
      '<p class="dek">Six chapters. Forty living frames. One uniform that refuses to sit still.</p></header>' +
      '<ol class="lbv-chapter-grid">' + cards + '</ol></div>';
  }

  function renderTitle(spread, frames, chapter) {
    const cover = frames[0];
    if (!cover || !chapter) return '';
    return '<div class="lbv-title">' + openable(cover, frames, {
      ken: true, eager: true, inner:
        '<div class="veil"></div><div class="copy"><p class="k">' + esc(spread.kicker) + '</p><h2>' + esc(spread.title) + '</h2>' +
        '<p class="body">' + esc(spread.body) + '</p><p class="sub">' + esc(chapter.subtitle) + '</p></div>'
    }) + '</div>';
  }

  function renderGate(spread, frames) {
    const cover = frames[0];
    if (!cover) return '';
    const shop = cover.shopHref
      ? '<a class="lbv-shop-link" href="' + esc(cover.shopHref) + '">' + esc(cover.shopLabel || 'Shop') + ' →</a>'
      : '';
    return '<div class="lbv-gate">' + openable(cover, frames, { ken: true, eager: true, inner: placeKicker(cover) }) +
      '<div class="lbv-gate-copy"><p class="k">' + esc(spread.kicker) + '</p><h3>' + esc(spread.title) + '</h3><p>' + esc(spread.body) + '</p>' + shop + '</div></div>';
  }

  function renderDiptych(frames) {
    return '<div class="lbv-diptych">' + frames.slice(0, 2).map(function (f, i) {
      return openable(f, frames, { ken: i === 0, eager: true, inner: placeKicker(f) });
    }).join('') + '</div>';
  }

  function renderTrio(frames) {
    return '<div class="lbv-trio">' + frames.slice(0, 3).map(function (f, i) {
      return openable(f, frames, { eager: i === 0, inner: placeKicker(f) });
    }).join('') + '</div>';
  }

  function renderSheet(frames) {
    return '<div class="lbv-sheet">' + frames.slice(0, 6).map(function (f) {
      return openable(f, frames, { inner: '<div class="lbv-sheet-cap"><p>' + esc(f.title) + '</p></div>' });
    }).join('') + '</div>';
  }

  function renderPortrait(spread, frames) {
    const cover = frames[0];
    if (!cover) return '';
    return '<div class="lbv-portrait"><div class="lbv-portrait-copy"><p class="k">' + esc(spread.kicker) + '</p><h3>' + esc(spread.title) + '</h3><p>' + esc(spread.body) + '</p></div>' +
      openable(cover, frames, { ken: true, eager: true, inner: placeKicker(cover) }) + '</div>';
  }

  function renderColophon() {
    return '<div class="lbv-colo"><p class="k">Colophon · Vol. 01</p><h2>End of<br>the book.<br><span>Not the uniform.</span></h2>' +
      '<p class="dek">The Living Book is Melato’s editorial of record — frames grouped the way a night actually happens: the set, the city, the origin, the hour after, the object, the exit.</p>' +
      '<div class="lbv-colo-grid"><a class="lbv-colo-card gold" href="' + esc(SHOP.tracksuits) + '"><p class="k">The Uniform</p><h3>Shop tracksuits</h3><p>Velour, bamboo, intent. Limited colourways.</p></a>' +
      '<a class="lbv-colo-card ghost" href="' + esc(SHOP.new) + '"><p class="k">The Latest Drop</p><h3>New arrivals</h3><p>When it’s gone, it files itself in the archive.</p></a></div>' +
      '<div class="lbv-colo-links"><a href="' + esc(SHOP.home) + '">melato.ca</a><a href="' + esc(SHOP.story) + '">Our story</a><a href="' + esc(SHOP.notes) + '">Melato notes</a>' +
      '<a href="' + esc(SHOP.dress) + '">Dress Code Violation</a><span>Ottawa · Montréal · Toronto · Meknes · Rabat · Marrakech · Casablanca</span></div></div>';
  }

  function splashSrc(pair) {
    return frameSrc(pair[0], pair[1], 480);
  }

  function renderSplash(root) {
    const cols = DATA.SPLASH.map(function (col) {
      const imgs = col.concat(col).map(function (pair) {
        return '<img src="' + esc(splashSrc(pair)) + '" alt="">';
      }).join('');
      return '<div class="lbv-strip-col">' + imgs + '</div>';
    }).join('');
    const marquee = DATA.MARQUEE.concat(DATA.MARQUEE).map(function (item) {
      return '<span>' + esc(item) + '<i></i></span>';
    }).join('');
    root.innerHTML =
      '<div class="lbv-loader" aria-hidden="false"><div class="lbv-loader-logo">MELATO</div><div class="lbv-loader-bar"></div><div class="lbv-loader-text">The Living Lookbook</div></div>' +
      '<section class="lbv-hero" aria-label="The Living Lookbook">' +
      '<div class="lbv-hero-strip" aria-hidden="true">' + cols + '</div><div class="lbv-noise" aria-hidden="true"></div>' +
      '<div class="lbv-hero-content"><div class="lbv-hero-eye">Melato · Editorial Archive · Vol. 01</div>' +
      '<h1>THE<br><span>LIVING</span><br>LOOKBOOK</h1><p>Styled like Evidence.</p>' +
      '<button type="button" class="lbv-enter" data-enter>Enter the book</button>' +
      '<button type="button" class="lbv-down" data-enter aria-label="Open The Living Book"><small>Scroll</small><i></i></button></div>' +
      '<div class="lbv-marquee" aria-hidden="true"><div>' + marquee + '</div></div></section>';
  }

  function chapterLabel(spread) {
    if (spread.kind === 'contents') return 'Contents';
    if (spread.kind === 'colophon') return 'Colophon';
    const chapter = spread.chapterId ? chapterById(spread.chapterId) : null;
    return chapter ? chapter.roman + '. ' + chapter.title : 'The Living Book';
  }

  function pad(n) {
    return String(n).padStart(2, '0');
  }

  function bindBook(root) {
    let page = 0;
    let dir = 'fwd';
    let leaving = null;
    let lock = false;
    let reader = null;
    let contentsOpen = false;
    let touchStart = null;
    let lastWheel = 0;

    const book = document.createElement('div');
    book.className = 'lbv-book is-arming';
    let armed = false;
    window.setTimeout(function () {
      armed = true;
      book.classList.remove('is-arming');
    }, 600);
    book.innerHTML =
      '<div class="lbv-bookmark"></div>' +
      '<header class="lbv-chrome-top">' +
      '<button type="button" class="lbv-mark" data-home>MELATO</button>' +
      '<p class="lbv-subtitle">The Living Book</p>' +
      '<div style="display:flex;gap:8px;align-items:center">' +
      '<a class="lbv-shop-btn" href="/">Shop</a>' +
      '<button type="button" class="lbv-contents-btn" data-toc aria-label="Open contents">' + ICON.list + ' Contents</button>' +
      '</div></header>' +
      '<div class="lbv-stage"></div>' +
      '<button type="button" class="lbv-nav prev" data-prev aria-label="Previous page">' + ICON.left + '</button>' +
      '<button type="button" class="lbv-nav next" data-next aria-label="Next page">' + ICON.right + '</button>' +
      '<footer class="lbv-chrome-bot">' +
      '<button type="button" class="lbv-mob-nav" data-prev aria-label="Previous page">' + ICON.left + '</button>' +
      '<div class="lbv-folio"><p class="lbv-folio-ch"></p><p class="lbv-folio-n"></p></div>' +
      '<button type="button" class="lbv-mob-nav" data-next aria-label="Next page">' + ICON.right + '</button>' +
      '<p class="lbv-hint">Click a frame · arrows to turn</p></footer>' +
      '<div class="lbv-toc" hidden role="dialog" aria-modal="true" aria-label="Contents"></div>' +
      '<div class="lbv-reader" hidden role="dialog" aria-modal="true"></div>';
    root.innerHTML = '';
    root.appendChild(book);

    const stage = book.querySelector('.lbv-stage');
    const bookmark = book.querySelector('.lbv-bookmark');
    const folioCh = book.querySelector('.lbv-folio-ch');
    const folioN = book.querySelector('.lbv-folio-n');
    const hint = book.querySelector('.lbv-hint');
    const toc = book.querySelector('.lbv-toc');
    const readerEl = book.querySelector('.lbv-reader');
    const prevBtns = book.querySelectorAll('[data-prev]');
    const nextBtns = book.querySelectorAll('[data-next]');

    function paintToc() {
      const items = ['<li><button type="button" data-go="0"><span class="n">00 Contents</span></button></li>'].concat(
        CHAPTERS.map(function (ch) {
          return '<li><button type="button" class="row" data-jump="' + esc(ch.id) + '"><span><span class="roman">' + esc(ch.roman) + ' · ' + esc(ch.numeral) + '</span><h3>' + esc(ch.title) + '</h3></span><span class="thesis">' + esc(ch.thesis) + '</span></button></li>';
        })
      );
      items.push('<li><button type="button" data-go="' + (TOTAL - 1) + '"><span class="n">Colophon</span></button></li>');
      toc.innerHTML = '<div class="lbv-toc-head"><p class="k">Contents</p><button type="button" data-toc-close class="lbv-contents-btn">Close</button></div><ol class="lbv-toc-list">' + items.join('') + '</ol>';
    }

    function updateChrome() {
      const spread = SPREADS[page];
      folioCh.textContent = chapterLabel(spread);
      folioN.textContent = pad(page + 1) + ' / ' + pad(TOTAL);
      bookmark.style.width = ((page + 1) / TOTAL * 100) + '%';
      prevBtns.forEach(function (b) { b.disabled = page === 0; });
      nextBtns.forEach(function (b) { b.disabled = page === TOTAL - 1; });
      hint.textContent = spread.kind === 'contents' ? 'Choose a chapter' : 'Click a frame · arrows to turn';
    }

    function mountSpread(index, className) {
      const node = document.createElement('div');
      node.className = 'lbv-spread ' + className + ' dir-' + dir;
      node.innerHTML = renderSpread(SPREADS[index]);
      return node;
    }

    function go(next) {
      const clamped = Math.max(0, Math.min(TOTAL - 1, next));
      if (clamped === page || lock) return;
      lock = true;
      dir = clamped > page ? 'fwd' : 'back';
      leaving = page;
      const exitNode = mountSpread(leaving, 'exit');
      page = clamped;
      const enterNode = mountSpread(page, 'enter');
      stage.innerHTML = '';
      stage.appendChild(exitNode);
      stage.appendChild(enterNode);
      updateChrome();
      window.setTimeout(function () {
        if (exitNode.parentNode) exitNode.parentNode.removeChild(exitNode);
        enterNode.classList.remove('enter', 'dir-fwd', 'dir-back');
        enterNode.classList.add('is-current');
        lock = false;
      }, 480);
    }

    function openReader(frameId, siblingIds) {
      const ids = siblingIds.filter(Boolean);
      const list = (ids.length ? ids : [frameId]).map(frameById).filter(Boolean);
      const index = Math.max(0, list.findIndex(function (f) { return f.id === frameId; }));
      reader = { frames: list, index: index };
      paintReader();
    }

    function paintReader() {
      if (!reader) {
        readerEl.hidden = true;
        readerEl.innerHTML = '';
        return;
      }
      const frame = reader.frames[reader.index];
      if (!frame) return;
      const shop = frame.shopHref
        ? '<a class="lbv-cta" href="' + esc(frame.shopHref) + '">' + esc(frame.shopLabel || 'Shop this look') + ' →</a>'
        : '<p class="lbv-filed">Filed. Not yet for sale.</p>';
      const nav = reader.frames.length > 1
        ? '<button type="button" class="lbv-nav prev" data-rprev aria-label="Previous frame">' + ICON.left + '</button>' +
          '<button type="button" class="lbv-nav next" data-rnext aria-label="Next frame">' + ICON.right + '</button>'
        : '';
      readerEl.hidden = false;
      readerEl.setAttribute('aria-label', frame.title);
      readerEl.innerHTML =
        '<div class="lbv-reader-img">' + imgTag(frame, { eager: true, width: 1800, sizes: '100vw', className: '' }) +
        '<button type="button" class="lbv-reader-close" data-rclose aria-label="Close">' + ICON.close + '</button>' + nav + '</div>' +
        '<aside class="lbv-reader-aside"><p class="n">' + pad(reader.index + 1) + ' / ' + pad(reader.frames.length) + '</p>' +
        '<p class="lbv-chip">' + esc(frame.kicker) + '</p><h3>' + esc(frame.title) + '</h3>' +
        (frame.place ? '<p class="place">' + esc(frame.place) + '</p>' : '') +
        '<p class="cap">' + esc(frame.caption) + '</p>' +
        (frame.garment ? '<p class="garment">' + esc(frame.garment) + '</p>' : '') + shop + '</aside>';
      const closeBtn = readerEl.querySelector('[data-rclose]');
      if (closeBtn) closeBtn.focus();
    }

    function setContents(open) {
      contentsOpen = open;
      toc.hidden = !open;
      if (open) paintToc();
    }

    book.addEventListener('click', function (event) {
      if (!armed) {
        event.preventDefault();
        event.stopPropagation();
        return;
      }
      const enter = event.target.closest('[data-home]');
      if (enter) { go(0); return; }
      const tocBtn = event.target.closest('[data-toc]');
      if (tocBtn) { setContents(true); return; }
      const tocClose = event.target.closest('[data-toc-close]');
      if (tocClose) { setContents(false); return; }
      const prev = event.target.closest('[data-prev]');
      if (prev) { go(page - 1); return; }
      const next = event.target.closest('[data-next]');
      if (next) { go(page + 1); return; }
      const jump = event.target.closest('[data-jump]');
      if (jump) {
        const i = chapterPageIndex(jump.getAttribute('data-jump'));
        if (i >= 0) go(i);
        setContents(false);
        return;
      }
      const goBtn = event.target.closest('[data-go]');
      if (goBtn) {
        go(Number(goBtn.getAttribute('data-go')));
        setContents(false);
        return;
      }
      const openBtn = event.target.closest('[data-open]');
      if (openBtn) {
        openReader(openBtn.getAttribute('data-open'), (openBtn.getAttribute('data-siblings') || '').split(','));
        return;
      }
      const rclose = event.target.closest('[data-rclose]');
      if (rclose) { reader = null; paintReader(); return; }
      const rprev = event.target.closest('[data-rprev]');
      if (rprev && reader) {
        reader.index = (reader.index - 1 + reader.frames.length) % reader.frames.length;
        paintReader();
        return;
      }
      const rnext = event.target.closest('[data-rnext]');
      if (rnext && reader) {
        reader.index = (reader.index + 1) % reader.frames.length;
        paintReader();
      }
    });

    book.addEventListener('touchstart', function (event) {
      const t = event.changedTouches[0];
      touchStart = { x: t.clientX, y: t.clientY };
    }, { passive: true });
    book.addEventListener('touchend', function (event) {
      if (!touchStart || reader) return;
      const t = event.changedTouches[0];
      const dx = t.clientX - touchStart.x;
      const dy = t.clientY - touchStart.y;
      touchStart = null;
      if (Math.abs(dx) < SWIPE_PX && Math.abs(dy) < SWIPE_PX) return;
      if (Math.abs(dx) > Math.abs(dy)) {
        if (dx < 0) go(page + 1); else go(page - 1);
      } else {
        if (dy < 0) go(page + 1); else go(page - 1);
      }
    }, { passive: true });

    window.addEventListener('keydown', function (event) {
      if (reader) {
        if (event.key === 'Escape') { reader = null; paintReader(); }
        if (event.key === 'ArrowRight') {
          reader.index = (reader.index + 1) % reader.frames.length;
          paintReader();
        }
        if (event.key === 'ArrowLeft') {
          reader.index = (reader.index - 1 + reader.frames.length) % reader.frames.length;
          paintReader();
        }
        return;
      }
      if (contentsOpen && event.key === 'Escape') { setContents(false); return; }
      if (event.key === 'ArrowRight' || event.key === ' ' || event.key === 'PageDown') {
        event.preventDefault();
        go(page + 1);
      }
      if (event.key === 'ArrowLeft' || event.key === 'PageUp') {
        event.preventDefault();
        go(page - 1);
      }
      if (event.key === 'Home') go(0);
      if (event.key === 'End') go(TOTAL - 1);
      if (event.key.toLowerCase() === 'c') setContents(!contentsOpen);
    });

    window.addEventListener('wheel', function (event) {
      if (reader || contentsOpen) return;
      const now = Date.now();
      if (now - lastWheel < WHEEL_MS) return;
      if (Math.abs(event.deltaY) < 18 && Math.abs(event.deltaX) < 18) return;
      lastWheel = now;
      const dominant = Math.abs(event.deltaX) > Math.abs(event.deltaY) ? event.deltaX : event.deltaY;
      if (dominant > 0) go(page + 1); else go(page - 1);
    }, { passive: true });

    stage.appendChild(mountSpread(0, 'is-current'));
    updateChrome();
  }

  function boot() {
    const root = document.querySelector('[data-lbv-root]');
    if (!root || root.dataset.lbvReady === 'true') return;
    root.dataset.lbvReady = 'true';
    if (root.dataset.hideChrome === 'true') document.body.classList.add('lbv-hide-chrome');

    renderSplash(root);
    window.setTimeout(function () {
      const loader = root.querySelector('.lbv-loader');
      if (loader) {
        loader.classList.add('is-gone');
        loader.setAttribute('aria-hidden', 'true');
      }
    }, 1250);

    const enter = function () {
      if (root.dataset.lbvOpen === 'true') return;
      root.dataset.lbvOpen = 'true';
      cleanup();
      // Defer past the activating tap. Safari retargets that click onto
      // whatever replaces the splash — usually a dark chapter card — and
      // the reader jumps to a near-black title spread.
      window.setTimeout(function () { bindBook(root); }, 180);
    };
    root.addEventListener('click', function (event) {
      if (root.dataset.lbvOpen === 'true') return;
      if (event.target.closest('[data-enter]') || event.target.closest('.lbv-hero')) {
        event.preventDefault();
        event.stopPropagation();
        enter();
      }
    });
    root.addEventListener('touchend', function (event) {
      if (root.dataset.lbvOpen === 'true') return;
      if (event.target.closest('[data-enter]') || event.target.closest('.lbv-hero')) {
        event.preventDefault();
        enter();
      }
    }, { passive: false });
    const onKey = function (event) {
      if (event.key === 'Enter' || event.key === ' ' || event.key === 'ArrowDown') {
        event.preventDefault();
        cleanup();
        enter();
      }
    };
    const onWheel = function (event) {
      if (event.deltaY > 80) {
        cleanup();
        enter();
      }
    };
    function cleanup() {
      window.removeEventListener('keydown', onKey);
      window.removeEventListener('wheel', onWheel);
    }
    window.addEventListener('keydown', onKey);
    window.addEventListener('wheel', onWheel, { passive: true });
  }

  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', boot, { once: true });
  else boot();
  document.addEventListener('shopify:section:load', boot);
})();
